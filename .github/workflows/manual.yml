# This is a basic workflow that is manually triggered

name: CI Manual Pipeline

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        java: [17]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
      with:
        path: main
    - name: Set up JDK ${{ matrix.java }}
      uses: actions/setup-java@v3
      with:
        distribution: 'oracle'
        java-version: ${{ matrix.java }}
    - name: Build with Maven
      working-directory: main
      run: mvn test
    - name: Generate JaCoCo report
      working-directory: main
      run: mvn jacoco:report
    - name: Upload JaCoCo report
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: jacoco-report
        path: main/target/site/jacoco
    - name: Deploy contract test report to GitHub Pages
      if: success()
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: main/target/site/jacoco
        destination_dir: jacoco-report
    - name: Print site URL
      run: echo "JaCoCo Test Report published at https://<USERNAME>.github.io/<REPO>/jacoco-report"

